{
    "rules": {
      ".read": "auth != null",
      ".write": "auth != null",
      "falls": {
        "$fallId": {
          ".validate": "newData.child('timestamp').exists()"
        }
      }
    }
  }
  